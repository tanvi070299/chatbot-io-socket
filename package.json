{
  "name": "iochat",
  "version": "1.0.0",
  "description": "simple chat app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tanvi",
  "license": "ISC",
  "dependencies": {
    "express": "*",
    "socket.io": "^2.2.0"
  }
}
